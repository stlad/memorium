events {
    worker_connections 768;
}

http {
    
    include /etc/nginx/mime.types;

    server {
        listen 80;
        server_name nebula;

        location / {
            root /usr/share/nginx/html/;
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}

